## 当前项目做什么

- 提供从目标发现、计划治理、执行纠偏到最终交付的可组合 Codex Plugin，让模型在不阻塞清晰工作的前提下更早发现方向、数据、交付和验收遗漏。
- 将生命周期、目标发现、Plan、SubAgent、Git、验证、项目权威和汇报拆为各自负责单一判断的模块；工具能力通过独立 Skill 组合，不成为 Plugin 的内置前提。
- 让持续执行过程保持简洁可见，完成时清楚说明新增、修改、删除、实际操作、验证范围、未完成事项、风险和下一步。

## 为什么要做这个项目

我相信大模型的能力，也重视我自己的判断。模型应帮助我梳理问题、寻找依据、暴露容易忽视的决策、形成可执行和可复查的路线，并在证据要求改变路线时纠偏；只有真正需要我决定方向或授权时才打断。

## 项目约束

- 用户当前指令优先于 Skill 的默认流程。
- 公共 Plugin 保持工具无关；持久化、复盘和其他工具的命令与安装方式由各自 Skill 负责。
- 不通过兜底流程、重复状态机或死板模板掩盖设计问题。兼容只有在必要性得到证明且用户能感知决定时才能引入。
- 测试只覆盖当前有意义的行为边界：清晰目标不被流程阻塞、Plan 独立且按价值引入、No-Plan 可演进并携带有效上下文、SubAgent 委派有净收益和单一所有者、Git worktree 服从当前 Codex 配置、验证按风险扩展、项目权威与工作记录分离、完成汇报语义完整。
- 不为历史 Plan 或 Plugin 版本维护兼容矩阵，也不把 Skill 原文逐句固化成测试。
