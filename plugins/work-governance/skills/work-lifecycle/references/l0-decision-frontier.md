# L0 Decision Frontier

Use a frontier when one unresolved choice can materially change the work. Keep
it small:

- the decision to make;
- what changes if each path is chosen;
- the recommended default and why;
- the exact target blocked by the decision.

Do not ask for confirmation when the missing fact is discoverable locally or the
goal is already actionable.
