# L3 Execution

Execute the next authorized slice only. Preserve unrelated changes, use the
smallest useful diff, and tie every edit to the goal contract.

For Git work, load `work-governance:git-change-governance`. For code lookup,
use project instructions and CodeGraph only when structural code retrieval is
actually needed. For Obsidian, image, browser, remote, data, or product-design
scenes, load the relevant scene-specific skill instead of expanding lifecycle.

Diagnose ordinary command failures locally. Ask or escalate only when evidence
shows a material route change or missing authority.
